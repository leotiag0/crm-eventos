{
    "name": "crm-eventos-root",
    "version": "1.0.0",
    "description": "Sistema CRM Eventos - Root",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "build": "cd frontend && npm install && npm run build",
        "install:all": "npm install && cd frontend && npm install"
    },
    "dependencies": {
        "dotenv": "^16.4.7",
        "express": "^4.21.2",
        "http-proxy-middleware": "^3.0.3"
    }
}